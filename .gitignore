# Machine runtime — never versioned in the workshop (scoped per-experiment)
.em/
**/.em/

# Habitat-0 local runtime state
/.habitat-0/

# Bus runtime plumbing (mailboxes are ephemeral, re-created by setup)
inbox/
outbox/

# Node/build artifacts
node_modules/
dist/
build/
*.tsbuildinfo

# Python test artifacts
__pycache__/
*.pyc

# Secrets / local config
.env
.env.*
*.local

# OS/editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Bus journal (per-session host evidence)
.sessions/

# Active Unlazy work ledgers; completed evidence belongs with its owning experiment
/GATES.md
/PLAN.md
/gates/
